jenkins migration

Installing the CloudWatch Agent → puts the software on the EC2.
CloudWatchAgentServerPolicy → gives that software permission to write to CloudWatch.
You need both for the agent to function properly.




how to change port 8080 to other ?
sudo vi /etc/sysconfig/jenkins


JENKINS_PORT="8080"
JENKINS_PORT="9090"

sudo systemctl restart jenkins



sudo vi /etc/default/jenkins
HTTP_PORT=8080



| vCPU | Executors |
| ---- | --------- |
| 2    | 1-2       |
| 4    | 2-4       |
| 8    | 4-6       |
| 16   | 8-12      |


change no.of executors 
Manage Jenkins
  → Nodes
  → Built-In Node
  → Configure
  → Number of Executors

how to check no.of executors from jenkins server 
"nproc"



what if a node termed as temperory offline ?
Running jobs continue.
New jobs won't start on that agent.

------------------
reasons for temporary making node offline ?

Server maintenance
OS patching
Troubleshooting



----------------
                 Controller
              (Jenkins Server)
                     │
      ┌──────────────┼──────────────┐
      │              │              │
 Built-in Node    Agent-1       Agent-2
  Executors=1    Executors=2   Executors=4

      │              │              │
     Job A       Job B,C         Job D,E,F,G





pipeline {
    agent any

    parameters {
        string(name: 'BRANCH', defaultValue: 'develop')
        choice(name: 'ENV', choices: ['sit','uat','prod'])
    }
}


--------------


Jenkins Triggered
        │
        ▼
Git Clone
        │
        ▼
Maven Build
(mvn clean package)
        │
        ▼
target/*.jar created
        │
        ▼
Docker Build
(Dockerfile uses target/*.jar)
        │
        ▼
Docker Image Created
        │
        ▼
Docker Tag
        │
        ▼
ECR Login
        │
        ▼
Docker Push
        │
        ▼
Image Stored in ECR
        │
        ▼
Deployment Server Pulls Image
        │
        ▼
Container Started



------


Jenkins Server
│
├── Workspace
│   └── target/
│       └── plantos-uptime-processor-1.0.jar
│
└── Docker Engine
     │
     └── Docker Image
          └── /opt/
               └── plantos-uptime-processor.jar    --------->   this is completely in Docker/ECS Container not in Jenkins server. 
  

------------------------
example

FROM eclipse-temurin:21
LABEL maintainer="infinite-uptime.com"
ADD  ./target/plantos-uptime-processor*.jar /opt/plantos-uptime-processor.jar

EXPOSE 9307
ENTRYPOINT ["java","-jar","/opt/plantos-uptime-processor.jar"]




ECS Task Start
      ↓
Pull image from ECR
      ↓
Create container
      ↓
Read ENTRYPOINT
      ↓
Execute ENTRYPOINT



